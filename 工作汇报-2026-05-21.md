# 工作汇报 — 2026-05-21

> 项目：Polymarket 概率套利 Agent 系统
> 阶段：ML 模型接入 + Dry-run 验证 + 关键 bug 修复

---

## 今日核心成果

按您上次建议（用 transform 算 edge、不用新闻），完成了**从 LLM 决策到 ML 决策的完整切换**，并跑了 16 小时 dry-run。

### 1. 训练了 LightGBM 概率估算器（替代 LLM）

**数据**：本地 SQLite 缓存里的 470 个 Polymarket 市场、82 万笔历史交易（4 个月跨度，2026-01 至 2026-05）

**特征工程**（33 个）：
- 价格类：5 个时间窗 log-return、4 条均线、3 个均线比率、3 个波动率、2 个回撤、2 个标准化动量、1 个加速度、log_price
- 成交量类：3 个时间窗成交量、放量比率、z-score
- 链上独有：3 个买卖盘强弱指标（`buy_ratio_20`, `buy_ratio_60`, `buy_sell_imbalance_5`）
- 活跃度：2 个交易笔数、1 个蜡烛形态

**目标变量**：P(24h 后价格涨 >1%) — 二分类，cross-entropy loss

**模型**：LightGBM，500 棵树（early-stop 在 61 棵），num_leaves=31，learning_rate=0.05，feature_fraction=0.8，bagging_fraction=0.8

**Walk-forward 切分**：按时间排序，前 60% 训练、中间 20% 验证、最后 20% 测试

### 2. 回测结果

| Edge 门槛 | 触发数 | 胜率 | 平均 24h 收益 | Sharpe per trade |
|---|---|---|---|---|
| > 2% | 2,604 | 39.7% | +5.17% | +0.087 |
| > 4% | 2,488 | 39.6% | +5.50% | +0.091 |
| > 6% | 2,387 | 39.5% | +5.86% | +0.096 |
| > 8% | 2,269 | 39.8% | +6.38% | +0.102 |
| > 10% | 2,141 | 39.8% | +6.93% | **+0.108** |

**Top 5 特征（按 gain）**：
1. `dd_60` — 60h 最大回撤 🔥
2. `dd_120` — 120h 最大回撤
3. `price` — 当前价格
4. `ma_60` — 60h 均线
5. `ret_24h` — 24h 对数收益率

**关键洞察**：**回撤特征压倒性最重要**。这跟原 K 线 detector 找到的 `narrow_pullback`（回调收窄）是核心信号完全一致 — ML 自动学到了同样的规律，且用连续值表达更精细。

### 3. 实盘 Dry-Run（16 小时）

**配置**：`ESTIMATOR_MODE=ml_only`（跳过 Bull/Bear/Judge LLM 辩论，直接用 ML edge 决策）

**结果**：

| 维度 | 数值 |
|---|---|
| Cycle 数 | 89 |
| Scanner 候选总数 | 2,808 |
| 触发白名单信号 | **2** 次（都是 Portugal 夺世界杯）|
| Buy 决策 | **2** 次（每次 $25，1/4 Kelly + 5% 上限）|
| 实际开仓 | **1**（因 bug，第二次被 silently 吞了）|
| 当前 PnL | -0.46%（$25 → $24.88，开仓 17h 后）|

**Portugal 这笔决策详情**：
- 入场价：$0.0869
- ML 预测 24h 涨 >1% 概率：**0.348**
- 当前价：$0.0865（**几乎没动**）
- 模型预测错（label = 0：价格没涨 >1%）

### 4. 修复了一个关键 bug（实盘前必须修）

**症状**：同一个 market 在 12h 去重过期后被触发 2 次，但 SQLite 里只有 1 个仓位。

**根因**：`positions.token_id` 是 PRIMARY KEY，第二次 INSERT 触发 ON CONFLICT，只更新了 peak_price，**没记录第二个 $25 仓位**。

**后果**：dry-run 下只是日志不准；**实盘下会真下两个 $25 单子但只跟踪 $25 — 风控会失效**。

**修复**：
- Schema 改成 `id INTEGER PRIMARY KEY AUTOINCREMENT`
- `upsert_position` 改成**仓位合并**逻辑：发现同 token 已有开仓，则加权平均入场价、累加 size_usdc（这跟真实交易所的"加仓"行为一致）
- 加 5 个单元测试覆盖：单次开仓、同价加仓、不同价加权平均、peak_price 取最大、不同 token 独立、平仓后重开
- 加生产 DB 迁移逻辑，保留 Portugal 那条记录
- **33 个单元测试全过**

---

## 整体观察 — ML 模型的真实表现

### 触发率没改善（瓶颈仍在 detector）

之前预测"5-20 个 buy/天"，实测只有 1-2 个/天。**原因不是 ML，是 detector + 白名单**：

```
ML 模型只在 detector 触发的市场上工作
detector 一天才触发 4-8 次（实测 2 次/16h）
所以 ML 无法把"0 buy"变成"很多 buy"，只能变成"1-2 buy"
```

ML 真正改变的是：**触发后必下单**（之前 LLM 几乎全 skip）。

### 模型偏爱低价市场（潜在风险）

唯一触发的市场（Portugal $0.0869）是**极便宜的市场**。ML 训练时学到的规律可能是"低价 + 有回撤 → 24h 内反弹概率高"，但**这跟最终结算结果无关** — 一个 $0.05 的市场可能在 24h 内反弹到 $0.07，但最终仍归零。

如果用 ML 信号买入并持有到结算，可能仍然亏损。需要：
- 配合短期止盈（不能持有到结算）
- 或更换 label 为"最终结算结果"而非"24h 涨跌"

### Label 是 proxy，不是真实概率

模型预测的是"24h 后价格上涨 >1%"，**不是"事件最终结算成 YES"**。这意味着：

- ML 的 `p_true` 跟"市场隐含概率"（market price）**不是同一概念**
- 现在的 `edge = p_true - market_price` 在语义上是错的
- 但回测里 sharpe 正向，说明模型确实有信号，只是被"误用"为概率套利
- 实际更像是**短期动量预测**

---

## 给您看的关键数字

| 指标 | 数值 |
|---|---|
| 训练样本 | 34,107 |
| 训练用市场数 | 324 |
| 训练时间 | < 1 秒（CPU）|
| 单次推理成本 | 接近零 |
| 测试集 Sharpe per trade | **+0.10** |
| 测试集胜率 | 39.5%（edge > 6%）|
| 单笔预期收益 | +5.9% |
| 模型大小 | 几 KB |

---

## 下一步建议

按优先级排：

### 高优先级

1. **替换 label 为"最终结算结果"** — 拉 Polymarket 上已结算的 1000-2000 个市场，用真实 outcome 作 label。这样 ML 真的在估算事件概率而不是短期价格波动。

2. **解决"市场集中度"问题** — Portugal 反复被触发，但池子里其他市场都没动。需要：
   - 或者放宽 detector 阈值（接更多市场进 ML 评估）
   - 或者完全 bypass detector，把 33 个候选都丢给 ML

3. **缩短 horizon** — 预测 24h 太长，价格漂移小。可以试 6h、12h horizons，配合更激进的止盈止损。

### 中优先级

4. **试 Transformer / LSTM 架构** — LightGBM 已能找出一定信号，但只用了 last bar 的特征。时序模型能利用整个 K 线序列结构。

5. **加宏观特征** — 体育市场加 Pinnacle 赔率，加密市场加 BTC 价格，政治市场加民调指数。外部数据往往比纯技术指标更强。

6. **校准 confidence** — 现在的 "low/medium/high" 是按 edge 绝对值粗分的，应该用概率校准（如 Platt scaling）让 ML 输出真实置信度。

### 低优先级

7. **更细粒度的 Kelly** — 当前用 1/4 Kelly + 5% 上限。可以根据 ML confidence 动态调整 Kelly 系数。

8. **实盘准备**（钱包、USDC、approve）—— 必须等上述 1-3 都做完再考虑

---

## 给您的一句话

> 按您建议把 LLM + 新闻换成 LightGBM ML 模型，walk-forward 回测 sharpe +0.10、单笔预期收益 +5.9%（n=2,387）；16h dry-run 触发了 2 次决策（都是 Portugal），暴露并修复了一个会导致实盘资金 double 的 positions table PK bug。当前最大问题是 (a) label 是 24h 价格 proxy 而非真实结算结果，(b) detector + 白名单瓶颈未解决导致触发市场过于集中。下一步想拉历史已结算市场重新训练，并 bypass detector 让 ML 当门卫。

---

## 文件位置

- 数据 + 特征工程：[src/ml/features.py](src/ml/features.py)
- 训练脚本：[src/ml/train.py](src/ml/train.py)
- 模型推理（替换 LLM）：[src/probability/ml_estimator.py](src/probability/ml_estimator.py)
- Pipeline 集成（ml_only 模式）：[src/pipeline/main_pipeline.py](src/pipeline/main_pipeline.py)
- 修复后的 positions 表 + 迁移：[src/storage/db.py](src/storage/db.py)
- 单元测试（含 PK bug 回归测试）：[tests/test_position_aggregation.py](tests/test_position_aggregation.py)
- 模型 artifact：[data/ml_model.txt](data/ml_model.txt)

---

## 附录：回归测试（修 bug 后）

新加的 6 个测试场景（[tests/test_position_aggregation.py](tests/test_position_aggregation.py)）：

```
test_single_position_inserts_normally           ✓
test_second_buy_same_price_doubles_size         ✓
test_second_buy_different_price_weight_averages ✓   # 加权平均逻辑
test_peak_price_takes_max                       ✓
test_different_tokens_are_independent           ✓
test_reopen_after_close                         ✓   # 关键：平仓后重开
```

全套 **33 个测试通过**。
