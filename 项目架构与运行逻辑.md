# Polymarket Agent — 项目架构与运行逻辑

> **更新日期：** 2026-05-18
> **代码版本：** 含 27 单元测试、12h 去重、校准后的 detector
> **用途：** 读完这个文档就能定位任何一个文件、改任何一个参数

---

## 一、最高层视角：系统在做什么

```
每 10 分钟 →  扫 Polymarket 全市场 → 过滤可疑机会 → 检测 K 线异常
                                                        ↓
                                              如果检测到智慧资金信号
                                                        ↓
                                              Tavily 拉新闻给 LLM 喂料
                                                        ↓
                                              Claude 估算"真实概率"
                                                        ↓
                                              Edge = 真实概率 - 市场价
                                                        ↓
                                              如果 Edge > 6%:
                                              Bull Agent ⚔️ Bear Agent
                                                        ↓
                                              Risk Judge 综合判断
                                                        ↓
                                              熔断器：今天亏太多了？仓位太重了？
                                                        ↓
                                              下单（dry-run 只记账）→ 入 SQLite

副线每 30 秒 → 看持仓 → 止损/止盈/到期 → 平仓
```

---

## 二、目录结构地图

```
d:/hedge funding/
├── main.py              ← CLI 入口，所有命令的开关
├── config.py            ← 读 .env 配置成全局 SETTINGS
├── .env                 ← 你的 API key（不提交 git）
├── .mcp.json            ← Polymarket 文档 MCP server
├── data/
│   └── traces.db        ← SQLite 数据库（所有运行记录）
├── logs/
│   └── agent_YYYY-MM-DD.log
├── src/
│   ├── data/            ← 数据层
│   ├── scanner/         ← 模块 1：粗筛
│   ├── detector/        ← 模块 2：K 线信号
│   ├── probability/     ← 模块 3：概率估算
│   ├── agents/          ← 模块 4：多 Agent 辩论
│   ├── execution/       ← 模块 5：下单
│   ├── risk/            ← 模块 6：风控
│   ├── learning/        ← 模块 7：复盘
│   ├── pipeline/        ← 主线/副线调度
│   ├── notify/          ← Telegram 告警
│   ├── storage/         ← SQLite 封装
│   ├── backtest/        ← 历史回测
│   ├── cli/             ← status 命令实现
│   └── utils/           ← 工具函数
└── tests/               ← 单元测试
```

---

## 三、入口与配置

### [main.py](main.py) — 总入口

```
python main.py scan-once    ← 跑一次主线（调试用）
python main.py monitor-once ← 跑一次副线
python main.py run          ← 启动 24h 调度器（最常用）
python main.py status       ← 看当前状态
python main.py review       ← 每日复盘
python main.py backtest     ← 历史回测
python main.py --live ...   ← 启用真实下单（否则 dry_run）
```

核心是 `cmd_run()`：用 APScheduler 同时跑两个 job
- `main_loop`（10 分钟一次）= run_once 完整决策流程
- `monitor_loop`（30 秒一次）= 持仓监控

### [config.py](config.py) — 配置中心

读 `.env` 文件，把所有环境变量装进一个 `Settings` 对象。其他模块用 `from config import SETTINGS` 取值。包括：
- API keys（Anthropic、Tavily、Polymarket）
- 运行模式（dry_run / live）
- 资金参数（bankroll、单笔上限）
- 调度间隔
- 熔断阈值

---

## 四、数据层 [src/data/](src/data/)

### [polymarket_client.py](src/data/polymarket_client.py) — 所有 Polymarket API 都在这

| 方法 | 作用 |
|---|---|
| `list_active_markets()` | 拉所有活跃市场（**自动分页**，因为 Gamma 单页上限 100）|
| `to_markets()` | 把 JSON 转成内部 `Market` 对象（处理 YES/NO 双侧）|
| `fetch_price_history()` | 拉 K 线数据（`prices-history` 端点）|
| `fetch_market_trades()` | 拉成交流水（**用来重建成交量**，prices-history 不返回 volume）|
| `get_order_book()` | 当前订单簿（risk manager 用来报价）|
| `post_limit_order()` | 真实下单（仅 live 模式）|

### [volume.py](src/data/volume.py)

把 `/trades` 端点拉到的成交流水**按小时桶**累加，填回 Candle 的 `volume` 字段。这是为了让 `vol_trend` / `vol_spike` 信号能工作。

### [news_client.py](src/data/news_client.py) — Tavily 包装

`search(query)` 返回最近相关新闻。给 Claude 当上下文用。无 key 时自动 no-op。

### [types.py](src/data/types.py) — 内部数据结构

```python
Market          ← 一个市场（含 token_id、价格、到期日、成交量）
Candle          ← 一根 K 线（OHLC + volume）
DetectionResult ← detector 输出（触发了哪几类信号、score）
ProbabilityEstimate ← LLM 输出（p_true、reasoning、confidence）
TradeDecision   ← Risk Judge 最终决策（action、size、reason）
Position        ← 一个持仓
```

---

## 五、七个核心模块

### 模块 1：扫描 [src/scanner/market_scanner.py](src/scanner/market_scanner.py)

**输入**：1000 个原始市场
**输出**：通过规则过滤的 ~20-30 个候选

`FilterConfig` 里的硬性规则：
- 价格 0.05-0.30（我们验证的 alpha 区间）
- 24h 成交量 ≥ $5,000
- 流动性 ≥ $10,000
- 价差 ≤ 5%
- 距到期 3-90 天
- 同一事件的 YES/NO 只保留**价格更低**的一侧（赔率更高）

### 模块 2：K 线检测器 [src/detector/smart_money.py](src/detector/smart_money.py)

**5 类智慧资金信号**：
1. `slow_grind` — 缓慢爬升（刻意建仓）
2. `vol_trend` — 成交量线性增长（持续入场）
3. `narrow_pullback` — 回调收窄（筹码稳定）
4. `breakout` — 横盘突破
5. `vol_spike` — 成交量突增

**白名单** `is_whitelisted_combo()`：必须包含 `narrow_pullback` AND (`breakout` 或 `vol_spike`)。其他组合（即使触发）也跳过。

这套阈值是经过 **300 市场回测 + 实盘诊断**校准过的。

### 模块 3：概率估算 [src/probability/](src/probability/)

#### [llm_client.py](src/probability/llm_client.py) — Claude 包装

`complete_json()` 发请求 → 解析返回的 JSON。**带容错的 JSON 提取器** `_extract_json()`：处理 markdown 代码块、前导文本、截断等情况。

#### [estimator.py](src/probability/estimator.py) — 多源融合

```
P_true = 0.40·P_llm + 0.20·P_base_rate + 0.25·P_news + 0.15·P_market
        (Claude 估)  (历史先验)        (新闻信号)   (市场价)
```

权重来自设计文档。`P_base_rate` 现在硬编码 0.18（低概率市场先验）。

#### [prompts.py](src/probability/prompts.py)

四套 prompt 模板：`PROBABILITY_PROMPT`、`BULL_PROMPT`、`BEAR_PROMPT`、`JUDGE_PROMPT`。给 Claude 看的"系统指令"。

### 模块 4：多 Agent 辩论 [src/agents/debate.py](src/agents/debate.py)

`DebateOrchestrator.run()` 流程：

```
1. 检查 edge >= 0.06 AND confidence != "low"
   ↓ 不满足就直接 skip（省 LLM tokens）
2. Bull Agent → JSON {thesis, key_evidence, conviction}
3. Bear Agent → JSON {thesis, key_evidence, conviction}
4. Risk Judge → JSON {action, size_multiplier, reason}
5. Kelly 公式算仓位（1/4 Kelly + 5% 单笔上限）
```

### 模块 5：执行 [src/execution/engine.py](src/execution/engine.py)

`ExecutionEngine.execute()`：
- dry_run 模式：生成 fake order_id，开仓记入 SQLite
- live 模式：调 `post_limit_order()` 真实下单
- 触发 Telegram 通知

**关键**：在 dry_run 下，"下单"只是数据库记录 — 但用的真实价格、真实 token_id、真实仓位计算。

### 模块 6：风控 [src/risk/](src/risk/)

#### [manager.py](src/risk/manager.py) — 持仓监控（副线）

`evaluate()` 遍历所有未平仓位，按 4 类规则判断：
- 止损：亏损 ≥ 40% → 平仓
- 止盈：盈利 ≥ 60% → 平仓
- 移动止损：从最高点回撤 ≥ 15%（盈利 20% 后启动）
- 到期：临近到期 ≤ 1 天 → 强制平仓

#### [circuit_breaker.py](src/risk/circuit_breaker.py) — 下单前熔断

`check()` 在每次 buy 之前调用，**任一触发就 block**：
1. 今日亏损 > 5% bankroll
2. 总敞口 > 50% bankroll
3. 同时持仓 ≥ 10 笔
4. 连续 5 笔亏损 → 冷却 1 小时

### 模块 7：学习层 [src/learning/loop.py](src/learning/loop.py)

`daily_review()` 每日复盘：
- 算每类信号的命中率、平均 PnL
- 调 Claude 分析失败案例
- 输出 JSON 报告（**目前不回写参数**，需要手工调整）

---

## 六、调度层 [src/pipeline/](src/pipeline/)

### [main_pipeline.py](src/pipeline/main_pipeline.py) — 主线核心

`run_once()` 完整流程（这是整个项目的心脏）：

```python
candidates = scanner.scan()           # 模块 1
for market in candidates:
    candles = fetch_history()
    detection = detector.detect()     # 模块 2
    if not triggered: continue
    if not whitelisted(signals): continue
    if recent_signal_within(12h): continue  # 12h 去重

    prob = estimator.estimate()        # 模块 3
    news = news.search()
    decision = debate.run()            # 模块 4

    if decision.action == "buy":
        if circuit_breaker.check():    # 模块 6
            execution.execute()        # 模块 5
```

### [monitor_pipeline.py](src/pipeline/monitor_pipeline.py) — 副线

每 30 秒跑一次 risk manager。

---

## 七、存储 [src/storage/db.py](src/storage/db.py)

`TraceStore` 封装 SQLite。**6 张表**：

| 表 | 内容 |
|---|---|
| `scans` | 每次扫描的统计（候选数、信号数、买入数） |
| `signals` | 每次触发白名单的信号 |
| `decisions` | 每次 LLM 决策（含 bull/bear summary 全文） |
| `orders` | 每次下单 |
| `positions` | 持仓表（含历史已平仓的） |
| `trades_cache` | 缓存的成交流水（避免重复 API 调用） |

每个 INSERT/UPDATE 都通过这一层 — 其他模块不直接写 SQL。

**关键方法**：
- `recent_signal_within(market_id, hours)` — **12h 去重**核心，避免同一市场反复调 LLM
- `insert_trades()` / `fetch_trades()` — trades 缓存层

---

## 八、通知 [src/notify/telegram.py](src/notify/telegram.py)

`Notifier` 类，4 个事件钩子：
- 开仓 / 平仓 / 熔断触发 / 错误

未配置 token 时 no-op，不影响主流程。

---

## 九、回测 [src/backtest/](src/backtest/)

```
[engine.py] BacktestEngine
  ├── build_universe() — 拉一批市场 + 全量 K 线
  └── run() — 滑窗回放 detector，每次触发生成一个 Trial

[metrics.py] aggregate(trials) → BacktestReport
  分桶：by_signal / by_combo / by_score / by_price_band / by_band_x_combo

[runner.py] run_backtest()
  入口，跑多个 horizon、导出 CSV、打印报告
```

---

## 十、辅助 [src/utils/](src/utils/) + [src/cli/](src/cli/)

| 文件 | 作用 |
|---|---|
| `utils/logger.py` | Loguru 配置（双 sink：stderr + 文件 + 30 天归档） |
| `utils/kelly.py` | Kelly 公式仓位计算 |
| `utils/math_utils.py` | 线性回归、平均数等 |
| `cli/status.py` | `python main.py status` 的实现 |

---

## 十一、一次完整决策的数据流（实战例子）

以 2026-05-18 02:17 那次 Seoul Mayor NO @ 0.205 为例：

```
T+0s    APScheduler 触发 main_pipeline.run_once()
T+1s    scanner.scan() 拉 1000 个市场 → 27 个候选
T+1-3s  for each 候选: fetch_price_history() + enrich_volume()
T+3s    detector.detect() 命中 narrow_pullback + breakout
        ✓ 通过白名单
        ✓ 通过 12h 去重检查
T+3s    record_signal()
T+4-5s  estimator.estimate():
          ├── news.search("Will Chong Won-oh win...")  ← Tavily
          ├── llm.complete_json(PROBABILITY_PROMPT)   ← Claude 给 0.42
          └── 加权融合 → P_true=0.299
T+5s    edge = 0.299 - 0.205 = +0.0935 ≥ 6% ✓
T+5-12s debate.run():
          ├── llm(BULL_PROMPT)    ← Bull conv 0.72
          ├── llm(BEAR_PROMPT)    ← Bear conv 0.72
          └── llm(JUDGE_PROMPT)   ← "skip, conviction parity"
T+12s   record_decision() 写入 SQLite
        action="skip"，不进 execute()
T+12s   continue 下一个市场
```

**LLM 调用次数**：1 次 estimate + 1 次 bull + 1 次 bear + 1 次 judge = **4 次**，约 $0.005 总成本。

---

## 十二、改参数速查

| 我想... | 看这个文件 |
|---|---|
| 改主流程顺序 | [main_pipeline.py](src/pipeline/main_pipeline.py) |
| 改过滤阈值 | [market_scanner.py](src/scanner/market_scanner.py) → `FilterConfig` |
| 改 K 线信号阈值 | [smart_money.py](src/detector/smart_money.py) → 各 `_xxx()` 方法 |
| 改概率加权 | [estimator.py](src/probability/estimator.py) → `EnsembleWeights` |
| 改 prompt | [prompts.py](src/probability/prompts.py) |
| 改 edge 门槛 / Kelly | [debate.py](src/agents/debate.py) → `EDGE_FLOOR` |
| 改止损/止盈 | [manager.py](src/risk/manager.py) → `RiskRules` |
| 改熔断规则 | [circuit_breaker.py](src/risk/circuit_breaker.py) + `.env` |
| 加新表 / 新查询 | [db.py](src/storage/db.py) |
| 加 CLI 命令 | [main.py](main.py) |

---

## 十三、核心设计决策

### 为什么主线/副线分开？
主线 10 分钟周期长（要扫全市场 + 多次 LLM 调用），不能影响持仓监控的灵敏度。副线 30 秒短周期，专门看持仓动态（价格、止损）。

### 为什么不用消息队列 / Celery？
单机 24/7 跑足够。APScheduler `max_instances=1` 保证不会重叠。复杂度往后再加。

### 为什么 SQLite 而不是 Postgres？
- 单机部署，零运维
- 所有读写在同一进程
- 容量足够（一年数据预计 <1GB）
- 文件备份 = 数据备份

### 为什么 dry_run 默认？
设计文档明确建议先纸面交易 2 周。即使代码完整，第一次实盘前必须人工确认。

### 为什么 LLM 选 Claude 而不是 GPT？
- Haiku 价格极低（$0.005/决策）
- 长上下文（200k）容得下新闻 + 历史
- prompt caching 进一步降本

### 为什么 K 线阈值校准过两次？
1. 第一版（设计文档原版）来自股票/加密市场 — 在 Polymarket 上 4/5 信号永不触发
2. 校准放宽后触发频率从 0/day → 5-10/day，sharpe 从理论 +0.6 回归到实测 +0.15

---

## 十四、当前已知限制

1. **学习层不闭环** — 复盘只生成报告，不自动调整权重
2. **LLM 偏 bullish** — Claude 倾向于估计概率偏高，没有 calibration 机制
3. **市场效率太高** — 主流 Polymarket 市场 Edge 通常 <3%，难过 6% 门槛
4. **实盘下单从未真跑过** — 代码就绪但需要钱包 + 测试单
5. **conviction 平局问题** — Bull/Bear 容易等量化（如 0.72 vs 0.72），需要更强的论点差异化指令

---

## 十五、参考资料

- [Polymarket-Agent交易系统设计.md](Polymarket-Agent交易系统设计.md) — 原始设计文档
- [Alpha验证报告.md](Alpha验证报告.md) — 回测 alpha 验证（含两版结果对比）
- [工作汇报-2026-05-13.md](工作汇报-2026-05-13.md) — 阶段性 mentor 汇报
- [README.md](README.md) — 快速启动指南
